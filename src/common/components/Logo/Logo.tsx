import BookIcon from "@mui/icons-material/ImportContacts";

import styles from "./Logo.module.scss";

const Logo = () => {
  return <BookIcon fontSize="large" className={styles.logo} />;
};
export default Logo;
