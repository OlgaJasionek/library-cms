export type Readers = {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  pesel: number;
  phoneNumber: number;
  isActive: boolean;
};

export type ReaderFormValues = {
  firstName: string;
  lastName: string;
  email: string;
  pesel: string;
  phoneNumber: string;
};
